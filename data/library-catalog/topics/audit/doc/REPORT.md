# AUDIT-DOC · 文档-实况一致性审计（终审④系统化）

**范围**：`topics/README.md`、`ACCEPTANCE-REPORT.md`、`INTERFACE-PROPOSAL.md`、`meta.json`（stages 各段）、工具链 `README.md`（v2→v3 节）、`TOPIC-GRAPH-HANDOFF-20260918.md`（§8）。
**方法**：对每条数字/事实声称找实测来源——`np.load` 复算 CSR、JSON 复算 nodes/card_topics/meta、sha256 复核、按文档原样重跑 P4 命令。**全程只读**，唯一写入为本文件。
**基线时点**：本审计实测于 2026-09-18 19:5x（`date` 实证），产物为 **R1 修复轮后**状态。

> ⚠️ **时效声明（本审计期间文档被并发改写）**：审计窗口内实测到三份被审文档 mtime 前移——
> `README.md` 19:19:46→**19:55:23**（45,569→47,164 B）、`ACCEPTANCE-REPORT.md` 19:22:35→**19:55:06**（52,903→63,111 B）、
> `meta.json` 19:13:59→**19:48:03**（72,830→78,060 B）。**§1/§2 的行号已按 19:55 版本复核，仍逐条成立**；
> 但本报告是**时点快照**，引用前请先 `grep` 复核该行是否仍存在。R1 修复轮本身亦是审计窗口内落地（19:42–19:48）。

## 0. 实况基准（本次实测，作为全部比对的判据）

| 对象 | 实测值 | 备注 |
|---|---|---|
| `edges.csr.npz`（19:45:26） | nnz **99,268**；outdeg avg **19.1378**/med 20/p90 20/max 20/孤立 **3**；`indices` ∈[0,**5186**]；w min .1763 / med **.2292** / mean **.3647** / p90 **.8734** / max 1.0；w=1 → **8,010（8.07%）**；无自环 | R1 后 |
| `edges.csr.pre-r1.npz` | `indices` max **5,185**；w med **.4093** / mean **.5187** / p90 **1.0**；w=1 → **11,294（11.38%）** | R1 前备份 |
| `edges.variant-spec.csr.npz` | nnz **92,253**、avg outdeg **17.785**、孤立 **350** | 两文档均未给数字 |
| `topics-report.json`（19:46） | ①**0.918799**/med .9412 ②**0.991155**/med .9963 ③**0.920040**/med .9412；p50 **2.623**/p95 **3.305**/max **4.339** ms；wall **5.38s**；衰减 1 跳 **.3058** / 3 跳 **.0781**；**第 5 跳起饱和**；边权全局 .3647 / 最优路 .4093 | R1 后 |
| `topics-report-allseed.json` | samples **400**、`source_type=all`、pool 5187 → ①.9218 ②.9841 ③.9232 | **400 采样**，非全量 |
| `topics-report-exhaustive.json` | `exhaustive_sources` **5187** → ①.919449 ②.985561 | 真·全量 |
| `meta.json`（19:48:03） | `current_stage=P3`；`stages.P2.r1_fix` 已登记；P2 `edge_stats`/`self_test`（.9188/.9234）与 P3 `coverage` 均为 R1 后 | 本会话内实测 mtime 由 19:13 → 19:48 |
| `nodes.json` / `card_topics.jsonl` / `export-sample` | 5,187（kw4,838/cls349）/ 188,512 行 / 8 文件 **4,004,106 B** | 与文档一致 |

## 1. ❌ 不符（数字与实况冲突）

| # | 文档·行 | 声称 | 实测 | 建议改法 |
|---|---|---|---|---|
| 1 | `README.md:113`、`:311`、`:593`；`ACCEPT-RPT:68,120`；工具链 `README:158` | 权重 med **0.4093** / mean **0.5187** / p90 1.0；w=1 **11,294（11.38%）** | med .2292 / mean .3647 / p90 .8734；w=1 **8,010（8.07%）** | 上述行全部换成 R1 后值；并注明 11,294 属 `edges.csr.pre-r1.npz` |
| 2 | `README.md:311`、`:317`、`:318`、`:596`；`ACCEPT-RPT:69,239,350,390`；`INTERFACE:58`；工具链 `README:123` | p50 **1.986** / p95 **2.395** / max 2.67 ms；衰减 1 跳 **0.4427**、3 跳 **0.2401**；P3 总耗时 **7.5s** | p50 **2.623** / p95 **3.305** / max **4.339**；衰减 **0.3058 / 0.0781**；wall **5.38s** | 换 R1 后值；`ACCEPT-RPT:239/350` 是 §1–§8 未加"R1 前"标注的裸数字 |
| 3 | `README.md:106` | `indices` 实测 max **5,185** | 当前 max **5,186** | 改为 5,186（值域仍 ⊂[0,N) ✅），或注明为 R1 前读数 |
| 4 | `README.md:155`；工具链 `README:130` | 「6 跳覆盖 **0.8969** ≈ 12 跳 **0.8972** → 瓶颈不是跳数」 | R1 后 6 跳 = 12 跳 = **0.9188**（实际第 **5** 跳起饱和） | 结论仍成立但数字须换；"饱和跳"由第 9 跳改为第 5 跳 |
| 5 | `README.md:324`、`:571`；`INTERFACE:14,15,80,81`；工具链 `README:119` | ①「0.8972 **<0.90**」；「与 v1 基线 0.902 **差 0.28pp**」 | R1 后 ① **0.9188**（median .9412，`pass=true`）**高于** 0.902，非"差 0.28pp" | 该对比已作废；须按 `ACCEPT-RPT:205/231` 的写法改为"不同图 + 不同口径，不作对比"，并删除"0.8972<0.90"的观察项定性 |
| 6 | `README.md:205` | `stages.P2.self_test` = `hop12_kw_seeds_mean=0.8972` / `all=0.9030` | 现为 **0.9188 / 0.9234** | 换值 |
| 7 | `README.md:547`；工具链 `README:188` | `topics-p3.sh` 断言「**①0.8972 < 0.90** 未被口径改写」 | 现 ①=0.9188 ≥0.90，该断言前提消失（脚本已于 19:47 改写） | 改为"① 必须与 ② 并列且不得被口径替换"，去掉 0.90 上下界假设 |
| 8 | `README.md:377`、`:381` | `around 56 --hops 2`: **157** 主题 / 书证据 **59,575**；`graph 56 --hops 2`: **157** 节点 / **1,787** 边 | 原样重跑：**242** 主题 / **38,115** 本 / 280.2ms；graph **242** 节点 / **1,649** 边 | R1 改变了保留边集 → 全部邻域样例须重跑（`p4-smoke.txt` 19:14 整体作废） |
| 9 | `INTERFACE:57`；工具链 `README:114,118,128,253` | 硬门槛 ② **0.9797**；覆盖链 `0.7744→0.8972→0.9030→0.9797` | ② **0.9912**（kw400）/ **0.9841**（all400）/ **0.9856**（穷举） | 换值；链式叙述标注"R1 前" |
| 10 | `ACCEPT-RPT:71`、`:381` | 「`acceptance/` 下**不存在** `topics-p5.sh`」 | `topics-p5.sh`（7,521 B）与 `acceptance-p5.log` 均**已存在** | §8 该条与 §7.1 该行标为"已由 §9 闭合"，或直接改写 |
| 11 | `ACCEPT-RPT:41`、`:183`、`:193` | 导出版样例「生成于 **19:15:35**、**7 文件 + manifest**、3.9 MB」 | 现 `manifest.json.generated_at = 19:46:33`，**8 文件 / 4,004,106 B** | 改为 19:46:33 版本（8 文件）；19:15:35 那次作废 |
| 12 | `ACCEPT-RPT:167` | `p4-smoke.txt`「**14 例**」 | 文件为 **15 例**（`README:369`/HANDOFF §8 亦写 15） | 统一为 15 |
| 13 | `ACCEPT-RPT:0.1 表`（17–25 行） | 脚本 sha256/字节快照：`topic_edges.py` 29,102 B@18:49、`topic_graph.py` 30,873 B@19:16、`topics-p2/p3/p4.sh` 8,750/7,706/11,006 B | 现为 30,226 B@19:42、**33,439 B@19:44**、**12,735/9,279/15,450 B@19:47** | R1 已改脚本；该"脚本版本对账表"须重做，否则 §6 复跑结论无法对账 |
| 14 | `meta.json.stages.P3.coverage.cluster_only.role` | 「严格观察项（**与 v1 基线 0.902 基本持平**）」 | 同一对象内 `mean=0.9188`、`pass=true`，**高于** 0.902 | 最权威文件内部自相矛盾：改 role 文本 |

## 2. ⚠️ 表述歧义（数字对不上口径，非纯错）

| # | 文档·行 | 表述 | 实测 | 建议 |
|---|---|---|---|---|
| A1 | `ACCEPT-RPT:223`（§3.4） | 「源点从"400 随机 kw"**扩到全部 5,187 节点**」 | `topics-report-allseed.json`：**samples=400**、`source_type=all`、`pool_size=5187` —— 是**池扩到全节点、样本仍 400** | **终审④命中的正是此条**。改为"源点池扩到全节点（仍 400 采样）"；真全量引 `topics-report-exhaustive.json`（5187 源点，①.9194/②.9856）。`ACCEPT-RPT:230/457/468` 已自纠，但 §3.4 正文未改 |
| A2 | `README:135,145,375,551`；`INTERFACE:315`；工具链 `README:114` | 表头/正文「**全节点源点**」 | 同为 400 采样；`README:135` 已补"仍是 400 采样"，其余处未补 | 全库统一为「全节点**池** 400 采样」，禁止单独写"全节点" |
| A3 | `README.md` §8 的 **R1** vs `ACCEPT-RPT` §10 的 **R1** | `README:571` 的 R1 是**风险条目编号**（P3 门槛）；`ACCEPT-RPT`/`meta.json` 的 R1 是**修复轮** | 同一份交付里同名异义 | 风险编号改 `RISK-1` 或把修复轮改 `FIX-R1` |
| A4 | `README:12,226`；`ACCEPT-RPT:83` vs `meta.json.wall_seconds` vs `baseline-p0.txt` | P0「实测 **18.2s**」 | 登记 `wall_seconds=18.2`，而报告正文自报「耗时 **17s**」 | 两数分属"进程 wall"与"脚本自报"，须并列标注 |
| A5 | `meta.json.stages.P2.r1_fix.before_after.weight_eq1_ratio.after` | **0.081** | `p2-build-report.json` / `edge_stats` = **0.0807** | 统一精度 |
| A6 | `edges.csr.pre-r1.npz.NOTE.txt:2` | 称源文件「生成于 **19:0x**」 | 备份副本 mtime **18:50**（`cp -p` 下即源 mtime），二者互斥 | 修正 NOTE 的生成时间或说明拷贝语义 |
| A7 | `ACCEPT-RPT:249/255` | 边权均值 **0.5148<1**（衰减读法段） | R1 后全局均值 **0.3647**（同行 §10.2 已改） | 该段与 §10 冲突，换 0.3647 |
| A8 | `p4-smoke.txt:1` | 抬头「四命令 **10 例**」 | 正文 15 例（⑪–⑮ 为后补） | 抬头更新为 15 例 |
| A9 | `README:369` 表头「12 跳覆盖（kw 源点）」等 | 未标注"R1 前/后"的调参矩阵（`p2-tuning.json` 18:51） | 6 组配置全部为 R1 前读数，`ACCEPT-RPT:128` 已加警示 | 在 `p2-tuning.md` 表头加"R1 前，未重算" |
| A10 | `edges.variant-spec.csr.npz` | 两文档只写"另存/不进发行" | 实测 nnz **92,253**、avg outdeg **17.785**、孤立 **350**（`README:96` 的 5,188/99,268 只描述主产物） | 补 variant 自身规模，避免被读成同规模 |

## 3. ✅ 一致（抽样复核通过）

- **P1 全量**：5,187（kw 4,838 / cls 349）、id 0–5186 连续、`term` 5,187/5,187 唯一、freq min1/p25 17/med29/p75 78/p90 267/max 11,534/零频 0、cluster 396 值(0–399)无 -1、`cls` 356 值(349 cls2 + 37 一级)、sources `B3481/A+B954/A403/C349`、`truncated=false`、停用词 988、三源 A1518→(429/894)、B 224,511→9,044→4,435、`seconds=54.3`、噪声率 0.04 —— 与 `README:60-87`、`ACCEPT-RPT:98-102`、HANDOFF §8-P1 逐项一致。
- **证据链**：188,512 行、尺寸分布 `[22573,32886,38925,34190,25063,34875]`、链接 **487,933**、空卡 22,573（11.97%）、含 cls2 卡 179,587 ✅。
- **P2 结构（与 R1 无关的部分）**：nnz 99,268、dtype int64/int32/float32、`indptr(5188,)` 首 0 末 99,268 单调不减、出度 ≤20、distinct 对 330,426 / kw-kw 245,779 / 同簇 9,167 / 层级边 4,832 / 无层级证据 kw 6、`card_topics` 与卡对齐 ✅。
- **P0 基线**：188,512 / K=10 / 1,885,120 / 互链 0.423 / 12 跳 0.902（med 0.938）/ 0.911@400 簇 69s / 3 分量 188,488(99.99%) / 397-400 / 衰减 .730/.636/.584 / 随机 0.408 —— 逐行对 `baseline-p0.txt` ✅；`baseline_sha256=ff2a32ad…f2fa06` 本次重算**逐字节一致** ✅。
- **导出版**：8 文件、`total_bytes=4004106`（对账目录求和一致）、`card_topics_ids` 1,951,732 B ÷4 = 487,933 ✅、manifest `topics` 段 5187/99268/19.138/20/188512 ✅。
- **验收日志**：p0–p5 六份 log 存在，末行 `PASS (pass=8/17/27/18/30 fail=0)` 与文档引用一致（**但见 §4-③**）。

## 4. 复现命令可用性

| # | 命令（文档原样） | 结论 |
|---|---|---|
| ① | `README:229`「`graph_report.py --data … \| tee ~/go/lib-catalog-data/topics/baseline-p0.txt`」 | ⚠️ 可跑，但会把 `meta.json.baseline_sha256` 锚定的基线文件**原地覆盖**；复现即摧毁对账锚点。建议 `tee` 到临时路径后再比对 sha256 |
| ② | `README §3.5 ①`「`search 机器学习 --top 5`」 | ❌ 文档记 1.4ms/2 命中、`match 0.80/0.60`；实测 **561.5ms + 近词扩展 8 个**（`best_match=0.9<1.0` 触发 auto）。该行是 p4-smoke 早期（扩展前）快照，照跑不可复现 |
| ③ | `README §3.5 ⑤/⑨`、`p4-smoke.txt` | ❌ 见 §1-8（157→242 等） |
| ④ | `README §3.3`「`topic_edges.py --data …` 参数即默认值」 | ✅ argparse 与 `p2-build-report.json.params` 一致（`max-out 20`/`cap 50`/`combine sum`/`rank-by assoc`）；实测 `seconds=13.9` vs 文档 14.0s（可接受） |
| ⑤ | `README §3.2`「正式参数即默认值」 | ✅ 且 `--min-df` docstring=8 / argparse=**12** 的 R5 冲突**仍然存在**（`topic_build.py:11` vs `:416`），文档已如实标注 ✅ |
| ⑥ | `README §7` 五脚本 | ⚠️ 脚本已于 19:47 改写，而 `acceptance-p2/p3/p4.log` 仍是 19:21–19:22 的 **R1 前**运行留痕（`W_EQ1=11294`、`ST_HOP12=0.8972`、`P50=1.986`、`DECAY_HOP1=0.4427`）。「五脚本全绿」**无法从现有 log+脚本配对复核**，须重跑覆盖 log |

## 5. 计数摘要

| 判定 | 条数 | 说明 |
|---|---|---|
| ❌ 不符 | **14** | §1 表；其中 **8 条**（#1,2,4,5,6,7,9,11）集中在"R1 前数字未同步"，另 6 条为留痕/脚本/样例失效 |
| ⚠️ 歧义 | **10** | §2 表；**A1/A2 即终审④"全节点表述不符实"**，另有 4 条命令/留痕时效问题（§4-①②⑥ + A9） |
| ✅ 一致 | **6 组 / 约 60 项** | §3 表：P1 全量、P0 基线+sha256、P2 结构量、证据链、导出版、日志末行 |
| 文档破损度 | — | `ACCEPTANCE-REPORT`（含 §10，R1 后 15 处标记 / 18 处 R1 前残留）与 `README`（头部 1–38 行、§2.2、§3.4 已补 R1；§2.4/§7/§8/§9 未补）为**半更新**；`INTERFACE-PROPOSAL`、工具链 `README`、HANDOFF §8 为**零更新**（post-R1 标记数 = 0） |

**最紧要三条**：① 三份对外文档的 12 跳覆盖、权重分布、性能三组数字全部是 R1 前值；② "全节点/全量"在 6 处被写成抽样（④）；③ `README §3.4`/`§7`/`§8`/`§9` 与本次 R1 后实况直接冲突，且 §8 风险编号 R1 与修复轮 R1 同名。

## 6. 未决问题（须作者/施工方裁决，本审计不擅自改判）

1. **口径归属**：R1 后 ① 自然过线（0.9188/0.9194）→ "① 只作观察项、不作门槛"的裁定前提已变，是否重新裁定 ① 的地位？（`meta.json.stages.P3.verdict` 已重写为"R1 前/后"双列，但门槛仍钉 ②）
2. **`meta.json` 为唯一 R1 后文档**，而 `stages.P3.coverage.cluster_only.role` 仍是旧文案（§1-14）——登记器是否需要"角色文本随数值联动校验"？
3. **留痕重跑范围**：`acceptance-p2/p3/p4.log` 是否重跑覆盖？若不覆盖，是否在 log 内注明"R1 前"以免被当作现值引用？
4. **`p2-tuning.md/json` 是否重算**：出度 20→30 的 "+0.6pp 已证伪"同样取自 R1 前矩阵，R1 后是否仍成立？（本审计未复算，无法断言）
5. **`docs-draft/` 三份过程稿**仍写"P4 ⏳ 未实现""`mock`（默认）"——保留作过程稿，还是加冻结声明？
6. **样例/产物再生成**：`export-sample`（19:46:33）已随 R1 重生成，`p4-smoke.txt`（19:14）、`p2-self-test.txt`（18:52）、`acceptance-p*.log`（19:21）三者未同步，是否统一重跑到同一时点？
7. **`edges.csr.pre-r1.npz.NOTE.txt` 时间自相矛盾**（§2-A6）：以哪个时点为准？
