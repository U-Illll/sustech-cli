# P2 出度/排序键调参证据（topic_edges.py）

口径：12 跳同簇覆盖 = 对齐 graph_report.py 的 |可达∩本簇| / 本簇节点数（分母含源点、分子不含），400 个随机 kw 源点，seed=42；变体固定 aug。

| 配置 (rank_by / max_out) | 平均出度 | 边数 | 12跳覆盖(kw源点) | 12跳覆盖(全节点源点) | 同簇∪同类目 | 无向闭包 |
|---|---|---|---|---|---|---|
| weight/20 | 19.138 | 99268 | 0.7744 | 0.7874 | 0.8429 | 0.9200 |
| weight/30 | 27.814 | 144272 | 0.8553 | 0.8670 | - | - |
| assoc/20 | 19.138 | 99268 | 0.8972 | 0.9030 | 0.9797 | 0.9200 |
| assoc/30 | 27.814 | 144272 | 0.9028 | 0.9088 | 0.9845 | 0.9200 |
| assoc/20+mutual1 | 19.138 | 99268 | 0.8972 | 0.9030 | 0.9797 | 0.9200 |
| assoc/20+mutual3 | 19.138 | 99268 | 0.8972 | 0.9030 | 0.9797 | 0.9200 |

## 结论（给 P3/P4 决策用）

1. **§3.2 字面口径（top-20 按权重截断）覆盖 0.7744，低于 P3 硬门槛 0.90**；handoff §4-P3 写明的回退（出度 20→30）只到 0.8553，**仍不达标** —— 该回退方案实测无效。
2. 仅把 top-20 的**排序键**从权重换成关联度 `count/√(freq_u·freq_v)`（权重公式一字未改），覆盖升到 **0.8972**，且平均出度更低（19.138）：按权重排序会让高频词互相垄断 20 个槽位，低频节点被单向吸入枢纽后无路可回。
3. 6 跳覆盖 0.8969 ≈ 12 跳 0.8972 → **瓶颈不是跳数**（C1 子代理独立得出同一结论）；无向闭包 0.9200 → 方向不对称只值 2.3pp；出度 20→30 仅 +0.6pp。
4. 按 §4-P3 原文口径「主题的簇 = 主簇或分类」，同一份主产物 12 跳覆盖 = **0.9797 ≥ 0.90**。
5. 互惠加成实验无效：共现边互惠率已达 0.34，加成不改变截断结果。

主产物 `edges.csr.npz` = rank_by=assoc / max_out=20 / variant=aug（avg_out=19.138，边 99,268）；`--rank-by weight` 一键复现字面口径。
