# VERIFY-FIX · 修复后三反例独立验证报告

**结论：三反例全部 PASS；全量横扫残余分歧 0（公式 0 / 计数 0 / 容差 0）。** 34/34 断言通过（`verify.py` exit 0）。

- **脚本**：`audit/verify-fix/verify.py`（自研，单次运行 ~6s）｜**本次全部原始数字**：`audit/verify-fix/verify.json`
- **验证对象（冻结快照，窗口内未变动）**：`edges.csr.npz` sha256 `509c285aac82b9a1`（19:45:26）｜
  `topic_graph.py` sha256 `7c993f3035639ae4`（19:59:48，682 行）｜`card_topics.jsonl` sha256 `161a6ab87dc8328e`
- ⚠️ **与任务书前提的偏差（如实登记）**：任务书写 `topic_graph.py` 为「19:44 修复版」，实测 mtime 为 **19:59:48**，
  与 `audit/graph/REPORT.md` 记录的 **R1.1**（终审⑥证据修复，645→681 行）落点一致。本报告验证的是**该 19:59 版本**；
  ③ 的真实图与内存反例均针对它，①②④ 针对 19:45 边表，两者口径无冲突。
- **方法**：期望值只由证据链（`card_topics.jsonl` + `cards.jsonl` + `nodes.json` + `index/clusters.npy`）
  按公开契约（README §3.2 + §10.1 R1 记录：`w1=log1p(min(共现,50))/log1p(50)`、`w2=0.3·log1p(同簇)/log1p(50)`、
  `w=min(1,w1+w2)`、`assoc=共现/√(freq_u·freq_v)`、`--variant aug`=`top-5 kw ∪ {cls2}`、截断键 `src↑→tie↓→w↓→dst↑`）
  **自行重算**；**未 import** `topic_edges.py`/`topic_build.py`，未读 `p2-build-report.json` 的结论。
  父链长度由本脚本**自己走 parent map** 得出 —— **不采信**被测方的 `path`/`hops_used`（`path_of` 按 `hops+1` 截断展示，会掩盖超跳）。
- **约束**：主体只读（三件套哈希开工/收工一致）；只写 `audit/verify-fix/`；无网络。

## ① 权重反例 PASS — settlement(168) ↔ entanglement(4232)，MMS 991000938579704181

| 项 | 独立重算 | 落盘 `edges.csr.npz` | 判定 |
|---|---|---|---|
| 共有卡 | 恰为该 MMS 一张（唯一） | — | PASS |
| 共现书数 / 同簇共现 | `1 / 0` → `w1=0.1762914343888821`、`w2=0` | — | PASS |
| 权重 | **0.1762914343888821**（Δ vs 0.1762914344 = **−1.1e−11**） | `168→4232` = **0.17629143595695496** = `float32(0.1762914344)` **逐位相等** | PASS |
| 方向 | 期望 `4232→168` 被 top-20 截断剔除 | 实存不存在（与期望一致） | PASS |

**有牙性**：修复前「双计」公式重算 = **0.2794153127**，与 `edges.csr.pre-r1.npz` 同边实测 **0.2794153094291687** 一致（≤1e−6），
与修复后值判别间距 **0.1031** —— 断言确有判别力，非平凡。

## ② 排序反例 PASS — 经济学(id=4) 邻域，assoc 主键

- **期望序 == 实际序**：出度 `20 == 20`，dst 序**逐位相同**（0 分歧），对应边权 `max|Δw| = 2.85e−08`。
- **终审反例场景复现**：`economist`(2139) 共现 10 / 同簇 8 / assoc **0.057807** → **在邻域，名次 15**，`w=0.777517`（与独立重算一致）；
  `economic`(120) 共现 39 / 同簇 0 / assoc **0.035250** → **让位**（名次 None），其 `w=0.938210` **本可入列** ——
  即它被挤出的唯一原因是 **assoc 升为主键**，而非权重不足。两词 assoc 与终审给出的 0.0578 / 0.0353 一致。
- **有牙性**：改回修复前键序（权重优先、assoc 仅 tie-break）重排，**第 15 位起分歧 4 处**，`economist` 在该序下**完全缺席**，
  其位置被 `id 5 / 189 / 648` 占据 —— 终审反例可在旧键序下逐位复现。

## ③ 超跳反例 PASS — 不变式：父链边数 ≡ layer ≤ hops

| 子项 | 实测 | 判定 |
|---|---|---|
| (a) `around 139 --hops 2` | CLI exit 0；**自研父链**：visited 251，**max = 2 边**，`layer≠父链长` **0 例**；CLI 自报 `hops_used` max=2、`len(path)` max=3 节点 —— 与自研一致 | PASS |
| (b) 内存 14 节点反例（请求 **12** 跳） | 修复后 **最深 12 边**（恰达边界，节点 13，layer=12），`layer≡父链长` 0 例脱钩 | PASS |
| (b) 有牙性 | **自研重建**的修复前语义（同跳内读已更新 `score` + 层内提级、layer 首访定型）在同一图上返回 **13 边**（超跳节点 13）→ 与 §10.1 记录的「13 边 → 12 边」吻合 | PASS |
| (c) 5 个随机种子（seed=20260918 抽） | seed 2523「S4」/1262「塑料」/5061「论与实验」/413「love」/4969「incentives」：`hops=2` 全部 max=2；`hops=12` 全部 **≤hops**、`layer` 0 例脱钩、0 环 | PASS |
| 检查器自测 | 注入 13 边纯父链 → 检出 13 边且判超 12 跳（检查器非空转） | PASS |

## ④ 全量横扫 — 残余分歧 **0**

| 分类 | 数量 | 说明 |
|---|---|---|
| **公式分歧** | **0** | 99,268 条边逐条对照独立重算期望权重，`|Δw| > 1e−6` 者 **0**；`max|Δw| = 2.98e−08` |
| **计数分歧** | **0** | 无证据边 **0**；完整复刻 top-20 后期望边集 99,268 == 实际边集 99,268（**缺 0 / 多 0**） |
| **容差分歧** | **0** | 99,268/99,268 落在 ≤1e−6 内（float32 落盘量化上限 6e−8，实测 2.98e−08） |

- 证据链独立统计与审计 NUM 对齐：卡 188,512｜distinct 对 **330,426**｜含 cls2 卡 179,587｜层级边 **4,832**。
- **3,974 条非分歧项（口径内，已单独登记）**：`kw → cls_of[kw]` 边经 ③ 层级边（`w=1.0`）与 ① 共现边去重取 max，
  故落盘 `w=1.0` 而纯共现权重 <1.0 —— 属既定口径，**不计入分歧**。
- **9 条层级边被截断（与审计 NUM「D1」同一现象，可独立复现）**：登记 4,832 → 存活 **4,823**。
  纯共现基权重层 0 分歧，说明该 9 条差额**完全由 top-20 截断规则产生、非数据或公式错误**；丢失清单：
  `global→F8`、`实验→TP3`、`方法→F2`、`时代→F2`、`computational→TP3`、`世纪→I2`、`principles→TP3`、`经典→TP3`、`仿真→TP3`。
  若「4,832 条层级边须全存」是硬要求，需**作者裁定**（改截断豁免或登记为 4,823），本报告只陈述可复现事实。

## 五、局限与残余风险（如实声明）

1. **契约来源**：本验证的**实现**独立，但**契约**取自文档/代码注释（§3.2 与 §10.1）。若文档与设计者真实意图有偏差，本套件与该偏差同向，**不能**发现。
2. **③(b) 的「修复前」对照是我自研重建**（据缺陷描述），非施工方原码 —— 工作树内**无 VCS 历史、无 pre-fix 副本**，故只能证明「该反例对符合所描述缺陷语义的算法有牙」，不能证明它逐字复现旧实现。
3. **③(c) 未压到边界**：真实图 `hops=12` 实测最深仅 **7–8 边**（图在第 5 跳起饱和），故真实数据上的边界压力来自 ③(a)(=2) 与 ③(b)(=12)；真实图 12 跳边界未被直接验证。
4. **③ 未覆盖 `--budget` 饱和 + 层内提级叠加**、`--threshold` 极值下的超跳；本次仅按任务书范围验证。
5. **并发写入环境**：验证窗口内 `audit/` 下另有多个审计子目录（decay/export/edgetype/repro/final-rerun 等）在被其他执行体改写；本报告仅绑定上述三个 sha256，**任何一件变动即需重跑**。

## 六、复跑方式

```bash
cd ~/go/lib-catalog-data/topics/audit/verify-fix
PYTHONPATH=~/go/pylibs python3 verify.py            # 全量 34 断言，~6s；exit 0 = 全过，1 = 有 FAIL
PYTHONPATH=~/go/pylibs python3 verify.py --json-out /tmp/v.json
```
断言清单与逐项实测值见 `verify.json`（`checks[]` / `counterexample_1_weight` / `counterexample_2_order` / `counterexample_3_overhop` / `sweep`）。
